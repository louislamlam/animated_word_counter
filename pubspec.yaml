name: animated_word_counter
description: An animated word counter widget that supports multiple languages with customizable styling, built on animated_flip_counter.
version: 1.1.0
homepage: https://github.com/louislamlam/animated_word_counter

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
